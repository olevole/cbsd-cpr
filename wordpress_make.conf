OPTIONS_UNSET+= NLS
OPTIONS_UNSET+= EXAMPLES
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= MAN
OPTIONS_UNSET+= X11

OPTIONS_SET+= PKGNG

#WITH_BDB_VER=6
WRKDIRPREFIX=/tmp
DEFAULT_VERSIONS+= php=55
DEFAULT_VERSIONS+= apache=24
DEFAULT_VERSIONS+= mysql=56
